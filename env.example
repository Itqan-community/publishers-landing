# Default tenant when no tenant can be resolved (e.g. visiting "/")
DEFAULT_TENANT_ID=saudi-center
# Environment Variables (Optional)

# This file shows available environment variables for the multi-tenant landing page.
# Copy this to .env.local and customize as needed.

# ===========================================
# DEPLOYMENT
# ===========================================

# The base URL of your application
# Used for generating absolute URLs in metadata
NEXT_PUBLIC_BASE_URL=https://yourdomain.com

# ===========================================
# BACKEND API CONFIGURATION (per tenant)
# ===========================================
#
# Per-tenant API URLs are in config/tenants.json under each tenant's "api" key:
#   "api": {
#     "development": "https://develop.api.cms.itqan.dev",
#     "staging": "https://staging.api.cms.itqan.dev",
#     "production": "https://api.cms.itqan.dev"
#   }
#
# Which URL is used:
# - Localhost (NODE_ENV=development): api.development
# - Netlify staging branch (NEXT_PUBLIC_ENV=staging): api.staging
# - Netlify production branch: api.production
#
# If a tenant has no "api" config, this fallback is used:
# NEXT_PUBLIC_API_URL=https://api.cms.itqan.dev

# API authentication key (if required)
# API_KEY=your-secret-api-key

# ===========================================
# TENANT CONFIG SOURCE
# ===========================================

# By default, tenant configs are loaded from config/tenants.json
# To use an API instead, uncomment and set these:

# API endpoint for fetching tenant configurations
# TENANT_API_URL=https://api.yourdomain.com/tenants

# API authentication key (if required)
# TENANT_API_KEY=your-secret-api-key

# ===========================================
# CACHING
# ===========================================

# Config cache TTL in seconds (default: infinite)
# CONFIG_CACHE_TTL=3600

# Redis URL for distributed caching (optional)
# REDIS_URL=redis://localhost:6379

# ===========================================
# ANALYTICS
# ===========================================

# Google Analytics ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Tag Manager ID
# NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# ===========================================
# CMS INTEGRATION
# ===========================================

# Default CMS base URL
# CMS_BASE_URL=https://cms.yourdomain.com

# CMS API key for content fetching
# CMS_API_KEY=your-cms-api-key

# ===========================================
# FEATURE FLAGS
# ===========================================

# Enable/disable tenant resolution strategies
# ENABLE_SUBDOMAIN_RESOLUTION=true
# ENABLE_PATH_RESOLUTION=true
# ENABLE_DOMAIN_MAPPING=true

# ===========================================
# DEVELOPMENT
# ===========================================

# Debug mode (shows console logs)
# DEBUG=true

# Disable config caching in development
# DISABLE_CONFIG_CACHE=true

# ===========================================
# SECURITY
# ===========================================

# Allowed domains for custom domain mapping (comma-separated)
# ALLOWED_DOMAINS=customdomain1.com,customdomain2.com

# Rate limit requests per minute per IP
# RATE_LIMIT_PER_MINUTE=60

# ===========================================
# NOTES
# ===========================================

# - NEXT_PUBLIC_* variables are exposed to the browser
# - Other variables are only available server-side
# - Don't commit .env.local to version control!

