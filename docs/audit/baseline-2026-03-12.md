# Baseline Audit – 2026-03-12

## Commands Run

- `npm run lint`
- `npm run build`

## Current Lint/Typecheck State

- Lint passes with warnings:
  - `components/audio/AudioPlayer.tsx`: `useMemo` missing dependency `selectedRecitation` (2 warnings).
  - `components/layout/GovernmentBanner.tsx`: `@next/next/no-img-element` warnings for `<img>`.
- TypeScript passes via `next build` (no type errors reported).

## Current Route Rendering Modes (from `npm run build`)

- `ƒ /` – dynamic server-rendered on demand.
- `○ /_not-found` – static.
- `● /[tenant]` – SSG with `generateStaticParams` (tenants: `default`, `publisher-1`, `publisher-2`, plus 2 more).
- `ƒ /[tenant]/hadiths` – dynamic SSR.
- `ƒ /[tenant]/qiraahs/[slug]` – dynamic SSR.
- `ƒ /[tenant]/recitations` – dynamic SSR.
- `ƒ /[tenant]/recitations/[recitationId]` – dynamic SSR.
- `ƒ /[tenant]/riwayahs/[id]` – dynamic SSR.
- `ƒ /api/download-audio` – dynamic API route.
- `○ /robots.txt` – static.
- `ƒ /sitemap.xml` – dynamic (currently logs dynamic-server-usage warnings due to `headers` and `no-store` fetch).

## Known Build-Time Warnings

- Dynamic server usage warnings for `/sitemap.xml` because:
  - `headers()` is called in the sitemap path (indirectly via helpers).
  - `no-store` fetch is used when generating sitemap recitation URLs.

These will be addressed in later phases (SEO + caching strategy).

## Lighthouse/Core Web Vitals – To Be Filled Manually

Run Lighthouse (or WebPageTest) in a real browser against the deployed app and record:

- **Tenant Home**:  
  - URL: `<PROD_OR_STAGING_TENANT_HOME_URL>`  
  - Metrics: LCP, INP, CLS, TTFB, Performance score.

- **Recitations List**:  
  - URL: `<TENANT_BASE>/recitations`  
  - Metrics as above.

- **Recitation Detail**:  
  - URL: `<TENANT_BASE>/recitations/<RECITATION_ID>`  
  - Metrics as above.

- **Hadiths Page**:  
  - URL: `<TENANT_BASE>/hadiths`  
  - Metrics as above.

> Replace placeholders with actual URLs and paste raw Lighthouse JSON or screenshots as needed.

