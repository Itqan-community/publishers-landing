import { RecitationDetailSkeleton } from '@/components/ui/Skeletons';

/**
 * Loading state for recitation detail page
 * Shows skeleton that matches the actual page layout
 */
export default function RecitationDetailLoading() {
  return <RecitationDetailSkeleton />;
}
